# Pattern 1 - Square Pattern

## Problem
Print a square pattern of `N × N` stars.

## Approach
- Use an outer loop to print each row.
- Use an inner loop to print `N` stars in every row.
- Move to the next line after printing each row.

## Time Complexity
O(n²)

## Space Complexity
O(1)

## Language
Java